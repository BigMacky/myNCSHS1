<p id="notice"><%= notice %></p>

<h1>Listing Class Sections</h1>

<table>
  <thead>
    <tr>
      <th>Section name</th>
      <th>Room number</th>
      <th>Academic track</th>
      <th>Adviser</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @class_sections.each do |class_section| %>
      <tr>
        <td><%= class_section.section_name %></td>
        <td><%= class_section.room_number %></td>
        <td><%= class_section.academic_track %></td>
        <td><%= class_section.adviser %></td>
        <td><%= link_to 'Show', class_section %></td>
        <td><%= link_to 'Edit', edit_class_section_path(class_section) %></td>
        <td><%= link_to 'Destroy', class_section, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Class section', new_class_section_path %>
