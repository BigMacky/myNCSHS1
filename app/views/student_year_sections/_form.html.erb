<%= form_for(@student_year_section) do |f| %>
  <% if @student_year_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_year_section.errors.count, "error") %> prohibited this student_year_section from being saved:</h2>

      <ul>
      <% @student_year_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_lrn %><br>
    <%= f.text_field :student_lrn %>
  </div>
  <div class="field">
    <%= f.label :year_level %><br>
    <%= f.number_field :year_level %>
  </div>
  <div class="field">
    <%= f.label :section_name %><br>
    <%= f.text_field :section_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
