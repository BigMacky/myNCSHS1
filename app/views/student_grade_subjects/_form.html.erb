<%= form_for(@student_grade_subject) do |f| %>
  <% if @student_grade_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_grade_subject.errors.count, "error") %> prohibited this student_grade_subject from being saved:</h2>

      <ul>
      <% @student_grade_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_lrn %><br>
    <%= f.text_field :student_lrn %>
  </div>
  <div class="field">
    <%= f.label :subject_title %><br>
    <%= f.text_field :subject_title %>
  </div>
  <div class="field">
    <%= f.label :quarter %><br>
    <%= f.number_field :quarter %>
  </div>
  <div class="field">
    <%= f.label :grade %><br>
    <%= f.text_field :grade %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
